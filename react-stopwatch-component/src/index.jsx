import React from 'react';
import ReactDOM from 'react-dom';
import Stopwatch from './stopwatch';

const stopwatch = <Stopwatch />;

ReactDOM.render(stopwatch, document.querySelector('#stopwatch-container'));
